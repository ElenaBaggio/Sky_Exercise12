tup = 'Hello'
print(len(tup))
# the length of tup is 5 because tup is a string with 5 letters

tup = 'Hello',
# adding a comma at the end makes this a tuple!
# so the length of tup will now be 1
print(len(tup))

